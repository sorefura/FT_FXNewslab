[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "fx-swap-bot-boundaries"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["fx-core>=0.1.0", "fx-signal-store>=0.1.0"]

[tool.setuptools.packages.find]
where = ["src"]

